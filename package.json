{
  "name": "portfolio-api",
  "version": "1.0.0",
  "private": true,
  "description": "API for Shingu Akira Portfolio (TypeScript + Go)",
  "workspaces": [
    "ts",
    "packages/*"
  ],
  "scripts": {
    "dev:ts": "npm run dev --workspace=portfolio-api-ts",
    "build:ts": "npm run build --workspace=portfolio-api-ts",
    "test:ts": "npm run test --workspace=portfolio-api-ts",
    "test": "npm run test:ts"
  }
}
